version: '3.8'

services:
  assistant:
    build: .
    container_name: assistant
    volumes:
      - ./.env:/app/.env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    stdin_open: true
    tty: true
