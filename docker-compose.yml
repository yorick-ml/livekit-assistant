version: '3.8'

services:
  assistant:
    build: .
    container_name: assistant
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    stdin_open: true
    tty: true
    network_mode: "host"
